nav.breadcrumbs
  - swift.path_pages[1..-1].each do |p|
    a href=(p.path.blank? ? swift.uri : p.path)
      = p.parent || p.new? ? p.title : t('home')
    = ' '
    - if @opts[:spacer] && p != swift.path_pages.last
      = @opts[:spacer] + ' '
