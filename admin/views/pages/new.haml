.block
  = partial 'base/edit_operations'
  .content
    %h2.title
      = pat(:new)
      = mt(:page)
    .inner
      - form_for :object, url(:pages, :create), :class => :form do |f|
        = partial "pages/form", :locals => { :f => f }
