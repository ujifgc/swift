.block
  = partial 'base/index_operations'
  .content
    %h2.title
      = pat(:all)
      = mt(@models)
    %form.inner.multiple#multiple-pages{ :action => url(@models, :index)/:multiple, :method => :post }
      %table.table
        %thead
          %tr
            %th.first= mat(@model, :id)
            %th= mat(@model, :title)
            %th= mat(@model, :slug)
            %th= mat(@model, :path)
            %th= mat(@model, :published)
            %th.last= check_box_tag :check_all, :checked => false
        - @objects.each do |object|
          %tr
            %td.first= object.id
            %td= link_to object.title, url(@models.to_sym, :edit, :id => object.id)
            %td= object.slug
            %td= object.path
            %td= mk_light object
            %td.last= check_box_tag :"check_page[#{object.id}]", :checked => false

- content_for :sidebar, partial("base/sidebar")
