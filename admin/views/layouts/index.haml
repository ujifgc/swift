.block
  = partial 'base/index_operations'
  .content
    %h2.title
      = pat(:all)
      = mt(:layouts)
    %form.inner.multiple#multiple-layouts{ :action => url(:layouts, :index)/:multiple, :method => :post }
      %table.table
        %thead
          %tr
            %th.first= mat(:object, :id)
            %th= mat(:object, :title)
            %th.last= check_box_tag :check_all, :checked => false
        %tbody
          - @objects.each do |object|
            %tr
              %td.first= link_to object.id, url(:layouts, :edit, :id => object.id)
              %td= object.title
              %td.last= check_box_tag :"check_layout[#{object.id}]", :checked => false
