.block
  = partial 'base/edit_operations'
  .content
    %h2.title
      = pat(:edit)
      = mt(:news_rubric)
    .inner
      - form_for :object, url(:news_rubrics, :update, :id => @object.id), :method => :put, :class => 'form edit', :id => "edit-news_rubrics" do |f|
        = partial "news_rubrics/form", :locals => { :f => f }
