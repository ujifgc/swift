.active-bonds
  - @bonds.each do |bond|
    .item
      - title = bond.child.title
      - data = { :model => bond.child_model, :id => bond.child_id, :title => title }
      = link_to "#{title} (#{data[:model]})", 'javascript:;', :data => data, :class => :pick, :onclick => 'return false'
