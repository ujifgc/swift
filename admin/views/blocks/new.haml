.block
  .secondary-navigation
    %ul.wat-cf
      %li.first=link_to pat(:list), url(:blocks, :index)
      %li.active=link_to pat(:new), url(:blocks, :new)
  .content
    %h2.title
      =pat(:new)
      =mt(:block)
    .inner
      -form_for :block, url(:blocks, :create), :class => :form do |f|
        =partial "blocks/form", :locals => { :f => f }

-content_for :sidebar, partial("base/sidebar")
