= partial 'base/index_operations'
form.inner.multiple#multiple-cat_cards action=url(:cat_cards, :index)/:multiple method=:post
  table.table
    thead
      tr
        th.first
        th= mat(:object, :title)
        th= mat(:object, :slug)
    tbody
      - @objects.each do |object|
        tr
          td.last= mk_checkbox object
          td= mk_edit object
          td= object.path
= partial 'base/index_operations_bottom'
